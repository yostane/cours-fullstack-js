version: "3.9"

services:
  postgres:
    image: postgres:14-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=dev_pwd
      - POSTGRES_USER=dev_user
      - POSTGRES_DB=awesomeapi
